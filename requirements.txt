# Required
networkx>=3.0          # graph metrics, Louvain community detection
scipy>=1.10            # networkx>=3 delegates pagerank to scipy; without it
                       # centrality falls back to a degree approximation

# Strongly recommended — the run degrades without these
pdfplumber>=0.10       # table extraction from results sections
pypdf>=4.0             # fallback text extraction, form fields

# Optional
PyMuPDF>=1.23          # image extraction with position data
pytesseract>=0.3       # OCR fallback for scanned PDFs

# System packages (not pip):
#   poppler-utils   -> pdftotext, pdffonts, pdfinfo, pdftoppm, pdfimages
#   tesseract-ocr   -> OCR for scanned documents
# Debian/Ubuntu:  sudo apt-get install poppler-utils tesseract-ocr
# macOS:          brew install poppler tesseract
